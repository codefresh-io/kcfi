metadata:
  kind: helmChart
  installer:
    type: helm
    helm:
      chart: external-nodes
      release: cf-external-nodes

kubernetes:
  namespace: codefresh
  #context: 
  #kubeconfig:

nodeRegistarJob:
  #dockerRegistry:
  image: codefresh/cli
  tag: latest

consul:
  address: cf-consul
  port: 8500

## Set runtime environments
runtimeEnvironments:
  name: default-nodes
  extends: system/default
  cluster: codefresh
  nodes:
  - address: 
    # port: 2376
    # protocol: http
    # alternateLoggerConf: false

cf-api-token: 

alternateLoggerConf:
  mongoUri: 
  redisUri:
